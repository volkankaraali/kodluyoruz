import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginBottom: 6,
  },
  input: {
    backgroundColor: '#ECEFF1',
    borderRadius: 10,
    padding: 10,
  },
});
